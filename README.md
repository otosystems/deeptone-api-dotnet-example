# deeptone-api-dotnet-example
Example app, streaming from a microphone and sending data do the wss API, works on windows.
Works on Windows, because of the NAudio dependency. 

# Run with env variable API_KEY
> dotnet run
